{
  "_comment": "Optional JSON mapping for manual image overrides (custom filenames). Most users should use the auto-scan method instead: just name the file with the URL hash shown in the GitHub Issue report (e.g. 'abc123.jpg') and drop it into public/data/image-overrides/. This JSON file is only needed if you want to use a custom filename that doesn't match the hash.",
  "overrides": {}
}
